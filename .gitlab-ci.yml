pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/index.html
    - cp -r icons public/icons
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
